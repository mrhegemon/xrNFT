import React, { Fragment } from 'react';

const Nav = () => {
  return (
    <Fragment>
      <header className="nav">
        <span className="logo">// Cache</span>
      </header>
    </Fragment>
  );
};

export default Nav;